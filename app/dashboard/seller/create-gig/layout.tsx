import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Create a Freelance Service Gig | OASIS',
}

export default function CreateGigLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
